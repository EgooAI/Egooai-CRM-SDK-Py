[project]
name = "egooai-crm-sdk-py"
version = "0.1.0"
description = "CRM SDK prototype with SQLModel-based customer management."
requires-python = ">=3.10"
dependencies = [
    "sqlalchemy",
    "sqlmodel",
]

[tool.uv]
package = false
